import AccountClient from "@/components/AccountClient";

export const metadata = {
  title: "My Account — Best Texas Display",
};

export default function AccountPage() {
  return <AccountClient />;
}
